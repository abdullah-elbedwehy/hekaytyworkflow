# Hekayati tools — Python deps for story_pipeline.py (PDF + image checks)
pillow>=10.0.0
reportlab>=4.0.0
pypdf>=4.0.0

# In-image text: the image model draws the page's Arabic inside the artwork, and
# the PDF carries the same string invisibly so copy, search and `verify` keep
# working. Neither Pillow nor ReportLab shapes or bidis Arabic on its own, so
# both packages are required to build a book.
arabic-reshaper>=3.0.0
python-bidi>=0.4.2
